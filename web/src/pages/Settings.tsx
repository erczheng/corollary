import { PageStub } from '../components/PageStub'

export function Settings() {
  return (
    <PageStub
      title="Settings"
      description="API key presence, risk limits with audit log, notification routing, data sources, and theme."
    />
  )
}
